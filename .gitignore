/target
/build.log
/build_phase4.log
target-deploy/
target-release-verify/
target-verify/
target-verify-alac/
target-win-verify/

# WiX installer outputs (packaging/build-installer.ps1)
*.msi
*.wixpdb
crates/*/target/

# pnpm 12 trial (feat/pnpm12-trial): pnpm-managed crate sources are symlinks into
# the pnpm store and must be ignored — the pipeline hashes every unignored file
# and rejects symlinks.
.pnpm/
node_modules/
